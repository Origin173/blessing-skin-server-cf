{
  "name": "blessing-skin-pages",
  "version": "0.1.0",
  "private": true,
  "description": "Blessing Skin Server — Cloudflare Pages (Workers + D1 + R2 + KV) 移植",
  "type": "module",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "wrangler pages dev web/dist",
    "build:web": "npm run build --workspace web",
    "deploy": "npm run build:lang && npm run build:lang-js && npm run build:web && wrangler pages deploy web/dist --project-name=blessing-skin",
    "deploy:preview": "npm run build:web && wrangler pages deploy web/dist --project-name=blessing-skin --branch=preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "db:migrate:local": "wrangler d1 migrations apply blessingskin --local",
    "db:migrate:remote": "wrangler d1 migrations apply blessingskin --remote",
    "build:lang": "node scripts/build-lang.mjs",
    "build:lang-js": "node scripts/build-lang-js.mjs",
    "build:all": "npm run build:lang && npm run build:lang-js && npm run build:web"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "fast-png": "^8.0.0",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.1101.0",
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "@cloudflare/workers-types": "^5.20260730.1",
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^22.10.0",
    "js-yaml": "^5.2.3",
    "mysql2": "^3.23.2",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.118.0"
  }
}
